{
    "name": "fullstack-app",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend start\""
    },
    "devDependencies": {
        "concurrently": "^8.0.1"
    }
}